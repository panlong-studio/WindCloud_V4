# 第四期测试记录

## 1. 文档说明

本文档记录本次对 WindCloud 项目第四期代码的实际测试过程、测试结果和测试结论。

测试目标：

1. 确认项目可以正常编译。
2. 确认服务端可以正常启动，并能连接数据库。
3. 确认第三期已有核心功能在第四期改造后没有明显回归。
4. 确认第四期新增的长短命令分离、JWT 传输认证、控制连接超时踢出功能可以工作。
5. 记录测试中发现的问题和当前限制。

---

## 2. 测试环境

### 2.1 测试时间

- 2026-04-23

### 2.2 测试目录

- 项目根目录：`/home/liwenshuo/my_project/WindCloud_V4`

### 2.3 关键环境

- 操作系统：Linux
- 数据库：MySQL
- 配置文件：`config/config.ini`
- 服务端监听地址：`127.0.0.1:9090`
- 数据库名：`netdisk_db`
- JWT 库：`l8w8jwt`

### 2.4 本次使用的测试数据

- 测试用户：`s42581528`
- 测试密码：`p42581528`
- 测试目录：`stage4d9258`
- 传输目录：`tx9258`
- 小文件：`stage4_upload_1776929258.txt`
- 大文件：`stage4_big_1776929258.bin`

说明：

1. 最开始尝试使用更长的测试用户名，但数据库 `users.username` 字段长度为 `varchar(20)`，因此改用更短用户名继续测试。
2. 大文件测试使用 `64MB` 文件，用于观察传输线程和控制线程是否真正分离。

---

## 3. 测试前确认

### 3.1 配置文件检查

已确认 `config/config.ini` 中的关键配置存在：

- `ip=127.0.0.1`
- `port=9090`
- `db_host=localhost`
- `db_user=root`
- `db_pwd=123456`
- `db_name=netdisk_db`

### 3.2 数据库连通性检查

执行 MySQL 连通性测试：

```sql
SELECT 1;
```

结果：执行成功，数据库可连接。

### 3.3 编译检查

执行命令：

```bash
make -j2
```

结果：编译通过，客户端和服务端均可生成。

---

## 4. 测试项与结果

## 4.1 服务端启动测试

### 测试步骤

1. 启动服务端 `./bin/server_app`
2. 检查服务端日志

### 测试结果

- 服务端成功读取配置
- 服务端成功初始化数据库
- 服务端成功初始化数据库连接池
- 服务端成功启动监听 `127.0.0.1:9090`
- 服务端线程池初始化成功

### 结论

- 通过

---

## 4.2 用户注册与登录测试

### 测试 1：超长用户名注册

#### 测试步骤

使用长度超过 20 的用户名执行注册。

#### 实际结果

- 注册失败
- 服务端日志显示：
  - `Data too long for column 'username' at row 1`

#### 结论

- 该结果符合当前数据库表结构限制，不作为第四期功能故障处理。

### 测试 2：正常注册

#### 测试步骤

执行：

```text
register s42581528/p42581528
```

#### 实际结果

- 客户端提示：`注册成功！请继续登录。`
- 服务端日志记录新用户注册成功

#### 结论

- 通过

### 测试 3：正确密码登录

#### 测试步骤

执行：

```text
login s42581528/p42581528
```

#### 实际结果

- 客户端提示：`登录成功`
- 服务端日志记录用户登录成功

#### 结论

- 通过

### 测试 4：错误密码登录

#### 测试步骤

执行：

```text
login s42581528/wrongpass
```

#### 实际结果

- 客户端提示：`密码错误`

#### 结论

- 通过

### 测试 5：重复注册同名用户

#### 测试步骤

再次执行：

```text
register s42581528/p42581528
```

#### 实际结果

- 客户端提示：`注册失败`
- 服务端日志显示数据库唯一键冲突：
  - `Duplicate entry 's42581528' for key 'users.username'`

#### 结论

- 通过

---

## 4.3 短命令回归测试

### 测试步骤

登录后依次执行：

```text
pwd
mkdir stage4d9258
ls
cd stage4d9258
pwd
touch stage4_upload_1776929258.txt
ls
rm stage4_upload_1776929258.txt
ls
cd ..
rmdir stage4d9258
ls
```

### 实际结果

- `pwd` 返回 `/`
- `mkdir stage4d9258` 返回 `创建文件夹成功`
- `ls` 能看到 `stage4d9258/`
- `cd stage4d9258` 返回 `进入目录成功`
- `pwd` 返回 `/stage4d9258`
- `touch` 返回 `创建文件成功`
- `ls` 能看到 `stage4_upload_1776929258.txt`
- `rm` 返回 `文件删除成功`
- 删除后 `ls` 返回 `当前目录为空`
- `cd ..` 返回 `进入目录成功`
- `rmdir stage4d9258` 返回 `删除目录成功`
- 最后 `ls` 返回 `当前目录为空`

### 结论

- 通过

说明：

第四期改造后，第三期已有的基础目录和文件短命令没有出现明显回归。

---

## 4.4 小文件上传测试

### 测试步骤

在客户端执行：

```text
mkdir tx9258
cd tx9258
puts stage4_upload_1776929258.txt
```

### 实际结果

- 客户端先提示：`上传任务已启动。`
- 随后输出：`正在计算文件哈希值...`
- 最终输出：`上传完成！`

服务端日志显示：

1. 新建了一条传输连接
2. 传输连接进入线程池
3. 工作线程处理上传请求
4. 上传完成后关闭传输连接

### 结论

- 通过

说明：

该测试说明：

1. 控制连接和传输连接已经分离
2. 上传任务确实通过独立传输连接进入线程池
3. 登录后返回的 JWT 已经可以支撑新传输连接完成身份识别

---

## 4.5 小文件下载测试

### 测试步骤

1. 先把本地同名源文件改名，避免客户端下载时直接判断“本地已存在完整文件”
2. 在客户端执行：

```text
gets stage4_upload_1776929258.txt
```

### 实际结果

- 客户端提示：`下载任务已启动。`
- 随后提示：`下载成功: stage4_upload_1776929258.txt (39 字节)`
- 本地下载后的文件与备份源文件执行 `cmp` 比较，结果一致

### 结论

- 通过

---

## 4.6 大文件上传期间短命令并行测试

### 测试步骤

在客户端连续输入：

```text
puts stage4_big_1776929258.bin
pwd
ls
```

### 实际结果

客户端输出顺序中可以看到：

1. 主线程先提示：`上传任务已启动。`
2. 随后立刻可以收到 `pwd` 的返回结果：`/tx9258`
3. 同时传输线程输出：`正在计算文件哈希值...`
4. 最后上传完成

之后再次执行 `ls`，可以看到：

```text
stage4_upload_1776929258.txt stage4_big_1776929258.bin
```

### 结论

- 通过

说明：

该测试说明第四期最关键的目标已经实现：

1. `puts` 不再阻塞控制连接
2. 长命令和短命令可以并行工作
3. 服务端可以同时处理控制命令和独立传输连接

---

## 4.7 大文件下载与完整性测试

### 测试步骤

1. 先把本地 `stage4_big_1776929258.bin` 改名保存
2. 在客户端执行：

```text
gets stage4_big_1776929258.bin
```

3. 下载完成后，对原文件和下载文件执行 `sha256sum`

### 实际结果

客户端下载成功：

```text
下载成功: stage4_big_1776929258.bin (67108864 字节)
```

文件哈希对比结果一致：

```text
3b6a07d0d404fab4e23b6d34bc6696a6a312dd92821332385e5af7c01c421351
```

### 结论

- 通过

说明：

该测试说明：

1. 大文件上传后，服务端保存的文件可被重新下载
2. 下载后的文件内容和原文件一致

---

## 4.8 控制连接空闲超时测试

### 测试步骤

1. 新开一个客户端并登录
2. 登录后不再发送任何命令
3. 空闲等待超过 30 秒
4. 再发送：

```text
pwd
```

### 实际结果

- 客户端输出：`接收服务端响应失败`
- 服务端日志记录：

```text
控制连接超时，准备关闭
```

### 结论

- 通过

说明：

当前时间轮对控制连接的空闲超时处理已经生效。

---

## 4.9 超时后传输连接行为测试

### 测试步骤

1. 保持已登录客户端空闲，等待控制连接被服务端超时关闭
2. 在该客户端先执行短命令：

```text
pwd
```

3. 确认短命令失败后，再执行：

```text
gets stage4_upload_1776929258.txt
```

### 实际结果

- `pwd` 返回：`接收服务端响应失败`
- 之后 `gets` 仍然可以启动传输线程
- 客户端提示：`文件已存在且完整，无需下载。`

### 结论

- 当前实现下，控制连接超时关闭后，短命令会失效，但客户端缓存的 JWT 仍可继续用于新建传输连接

### 分析

这说明当前“超时踢出”的实际范围是：

1. 控制连接被关闭
2. 控制连接上的短命令失效
3. 但 JWT 本身没有因为控制连接超时而立刻失效

如果第四期对“超时踢出”的要求只是控制连接级别超时，那么当前结果可以接受。

如果期望“超时后整套会话都失效”，那当前实现还不够，需要后续补充：

1. 服务端令牌黑名单
2. 服务端会话版本号校验
3. 更短的 JWT 有效期，或者刷新机制

---

## 5. 数据与日志验证

### 5.1 数据库路径记录检查

查询用户 `s42581528` 的 `paths` 表记录后，确认存在：

- `/tx9258`
- `/tx9258/stage4_upload_1776929258.txt`
- `/tx9258/stage4_big_1776929258.bin`

### 5.2 服务端日志检查

服务端日志中已经确认以下关键行为均出现：

1. 控制连接注册成功
2. 传输连接进入线程池
3. 工作线程处理 `puts`
4. 工作线程处理 `gets`
5. 控制连接超时关闭

### 5.3 客户端日志检查

客户端日志中已经确认以下关键行为均出现：

1. 登录成功
2. 控制命令正常收发
3. 传输线程新建连接
4. 上传与下载任务执行成功
5. 控制连接超时后短命令接收失败

---

## 6. 测试结论

本次测试结果如下：

1. 项目可以正常编译。
2. 服务端可以正常启动，并成功连接数据库。
3. 第三期已有短命令功能在第四期改造后基本正常。
4. 第四期的长短命令分离已经生效。
5. JWT 已经可以支撑独立传输连接完成认证。
6. 时间轮已经可以对控制连接进行空闲超时关闭。

总体结论：

- 第四期核心功能已经基本可用。

---

## 7. 当前发现的问题与后续建议

### 7.1 已确认的问题

#### 问题 1：控制连接超时后，JWT 仍可继续发起传输连接

这不是代码崩溃类错误，但它说明“超时踢出”的范围目前只覆盖控制连接，没有覆盖 JWT 的继续使用。

建议后续明确产品语义：

1. 如果“超时踢出”只要求控制连接断开，则当前实现可以保留。
2. 如果“超时踢出”要求整套登录态失效，则需要继续补机制。

#### 问题 2：客户端在控制连接超时后，没有主动提示用户重新登录

当前表现是用户再次发短命令时，客户端只会看到：

```text
接收服务端响应失败
```

建议后续优化为更明确的提示，例如：

- `控制连接已超时，请重新登录`

### 7.2 其他建议

1. 可以补一组专门的 JWT 负向测试，例如伪造 token、篡改 token、过期 token。
2. 可以补一组传输中断恢复测试，例如上传一半断开再重连。
3. 可以补一组多客户端并发传输测试，继续验证线程池表现。

---

## 8. 本次测试最终结论

本次测试表明，WindCloud 第四期代码已经完成核心目标验证，具备继续迭代的基础。

但“控制连接超时后 JWT 仍然可用”这一点需要在后续开发中明确是否符合预期，然后决定是否继续增强会话失效机制。

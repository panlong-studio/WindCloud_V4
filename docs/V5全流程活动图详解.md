# WindCloud_V5 全流程活动图详解

## 1. 文档目标

这份文档用**活动图**把 V5 代码中的主要执行流程拆开讲清楚。

这三份“全流程图解”文档的分工如下：

1. [V5全流程活动图详解.md](/home/liwenshuo/my_project/WindCloud_V4/docs/V5全流程活动图详解.md)
   重点看流程骨架、判断分支和循环结构。
2. `V5全流程时序图详解.md`
   重点看模块交互顺序、协议包顺序和调用顺序。
3. `V5全流程状态图详解.md`
   重点看核心对象的状态变化和状态切换条件。

本文的重点不是模块分层，而是：

1. 程序从哪里开始进入
2. 每条主链路经历了哪些判断
3. 每个阶段可能分出哪些分支
4. 分支最终如何回到主流程，或者直接结束

为了方便对照代码，本文中的活动图会尽量对应到实际文件：

- `src/server/core/server.c`
- `src/server/core/session.c`
- `src/server/core/worker.c`
- `src/server/net/time_wheel.c`
- `src/client/client.c`
- `src/client/client_command_handle.c`
- `src/client/client_puts.c`
- `src/client/client_gets.c`
- `src/client/multipart_meta.c`

这份文档重点解决“**流程骨架怎么跑**”的问题。  
如果要继续向下钻细节，建议再配合时序图文档和状态图文档一起看。

---

## 2. 阅读方法

活动图和时序图、状态图的关注点不同。

### 2.1 活动图看什么

活动图主要看：

1. 流程执行到哪一步
2. 这一步会做什么判断
3. 判断成功和判断失败分别往哪里走
4. 哪些流程会结束，哪些流程会回到循环入口

因此，活动图特别适合回答这类问题：

- 服务端启动时先做什么、后做什么
- 客户端登录成功后下一步会进入哪个循环
- 服务端怎么判断一条新连接是控制连接还是传输连接
- `puts` 和 `gets` 在哪一步分叉
- 第五期多点下载是先申请方案，还是先起线程
- 控制连接超时后是从哪里切出去的

### 2.2 本文为什么拆成多张活动图

如果把 V5 所有流程都硬塞进一张大图，会很难读。

所以本文按代码执行层次，拆成下面几部分：

1. V5 总体执行骨架
2. 服务端启动与退出流程
3. 客户端启动、登录与命令循环流程
4. 客户端命令分流流程
5. 服务端 `epoll` 主循环流程
6. 控制连接命令处理流程
7. 传输连接票据校验与分发流程
8. 上传 `puts` 活动图
9. 下载 `gets` 活动图
10. 工作线程执行传输任务流程
11. 时间轮超时处理流程

这样读的时候，可以先看总图，再顺着某一条分支钻进去。

图中的颜色按流程阶段区分：蓝色通常表示入口或准备阶段，黄色表示判断和分流，青色表示控制链或票据链，绿色表示传输和正常执行，紫色表示收尾，红色表示失败、中断或保留现场。

---

## 3. V5 总体执行骨架

先用一张总活动图把客户端和服务端的主要运行方式放在一起。

```mermaid
flowchart TD
    A[启动服务端]
    B[服务端初始化配置 日志 数据库 线程池 epoll 时间轮]
    C[服务端进入 epoll 主循环]
    D[启动客户端]
    E[客户端建立控制连接]
    F[客户端进入登录菜单]
    G{登录是否成功}
    H[进入已登录命令循环]
    I{用户输入什么命令}
    J[短命令 走控制连接]
    K[puts]
    L[gets]
    M[控制连接申请一次性票据]
    N[控制连接申请多点下载方案]
    O[客户端创建独立上传线程]
    P[客户端创建下载管理线程]
    Q[上传线程建立传输连接]
    R[下载分片线程建立传输连接]
    S[服务端线程池工作线程处理传输连接]
    T[上传完成或中断]
    U[下载全部完成或保留分片进度]
    V[控制连接继续等待下一条命令]
    W{控制连接是否超时或断开}
    X[客户端不自动重连 继续由用户决定后续操作]

    A --> B --> C
    D --> E --> F --> G
    G -- 否 --> F
    G -- 是 --> H --> I
    I -- 短命令 --> J --> V --> I
    I -- puts --> K --> M --> O --> Q --> S --> T --> V
    I -- gets --> L --> N --> P --> R --> S --> U --> V
    V --> W
    W -- 否 --> I
    W -- 是 --> X
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class A,B,C,D,E phase1
    class F,G,H,I,J phase2
    class K,L,M,N,O phase3
    class P,Q,R,S phase4
    class V,W,X phase5
    class T,U fail
```

这张总图只强调一件事：

> V5 的核心不是“多了几个模块”，而是命令从输入开始就分成控制链和传输链；`gets` 进一步拆成“方案申请 + 分片下载”两阶段。

---

## 4. 服务端启动与退出活动图

服务端入口是 `src/server/core/server.c` 的 `main()`。

## 4.1 服务端启动主流程

```mermaid
flowchart TD
    S0[进入 server main]
    S1[signal SIGPIPE ignore]
    S2[先按默认路径初始化日志]
    S3[读取 ip port log db timeout 配置]
    S4[按正式配置重新初始化日志]
    S5[init_database]
    S6{数据库结构初始化是否成功}
    S7[init_db_pool]
    S8{数据库连接池是否成功}
    S9[pipe]
    S10{pipe 是否成功}
    S11[fork]
    S12{fork 是否成功}
    S13[父进程注册 SIGINT 处理并 wait]
    S14[子进程 setpgid]
    S15[init_socket 创建 listen_fd]
    S16[init_thread_pool]
    S17[time_wheel_init]
    S18[申请 conn_map]
    S19[create_timer_fd]
    S20[epoll_create]
    S21[add listen_fd]
    S22["add pipe_fd[0]"]
    S23[add timer_fd]
    S24[进入 epoll_wait 主循环]
    SF[记录错误并退出]

    S0 --> S1 --> S2 --> S3 --> S4 --> S5 --> S6
    S6 -- 否 --> SF
    S6 -- 是 --> S7 --> S8
    S8 -- 否 --> SF
    S8 -- 是 --> S9 --> S10
    S10 -- 否 --> SF
    S10 -- 是 --> S11 --> S12
    S12 -- 否 --> SF
    S12 -- 父进程 --> S13
    S12 -- 子进程 --> S14 --> S15 --> S16 --> S17 --> S18 --> S19 --> S20 --> S21 --> S22 --> S23 --> S24
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class S0,S1,S2,S3,S4,S5 phase1
    class S6,S7,S8,S9,S10 phase2
    class S11,S12,S13,S14,S15 phase3
    class S16,S17,S18,S19,S20 phase4
    class S21,S22,S23,S24 phase5
    class SF fail
```

### 4.1.1 这张图要抓住的重点

#### A. 服务端仍然不是单进程死循环

代码里仍然保留：

```c
pipe(pipe_fd);
pid_t pid = fork();
```

也就是说：

1. 父进程负责接收 `SIGINT`
2. 父进程通过管道通知子进程退出
3. 子进程才运行服务器主逻辑

#### B. V5 的主线程不仅负责监听新连接

启动阶段已经能看出 V5 的主线程后面还要管理：

1. `timerfd`
2. 控制连接 `epoll`
3. 控制连接时间轮

这说明模型不是“主线程只做 `accept`”，而是：

> 主线程直接管理控制连接，线程池只处理传输连接

---

## 4.2 服务端退出活动图

```mermaid
flowchart TD
    E0[父进程收到 SIGINT]
    E1["向 pipe_fd[1] 写一个字节"]
    E2["子进程 epoll 监听到 pipe_fd[0] 可读"]
    E3[读取退出通知]
    E4[线程池加锁]
    E5[pool.exitFlag = 1]
    E6[清空队列中尚未处理的传输连接]
    E7[shutdown 工作线程正在处理的 fd]
    E8[唤醒全部工作线程]
    E9[关闭 listen_fd]
    E10[关闭全部控制连接]
    E11[join 全部工作线程]
    E12[destroy_thread_pool]
    E13[destroy_db_pool]
    E14[关闭 timerfd pipe epoll]
    E15[free conn_map]
    E16[close_log]
    E17[子进程退出]

    E0 --> E1 --> E2 --> E3 --> E4 --> E5 --> E6 --> E7 --> E8 --> E9 --> E10 --> E11 --> E12 --> E13 --> E14 --> E15 --> E16 --> E17
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class E0,E1,E2 phase1
    class E4,E5,E6,E7 phase2
    class E8,E9,E10 phase3
    class E11,E12,E13,E14 phase4
    class E15,E16 phase5
    class E3,E17 fail
```

### 4.2.1 退出流程为什么值得单独画

因为代码不是“只关主线程”，而是还要处理：

1. 队列中尚未开始的传输连接
2. 工作线程正在处理的传输连接
3. 主线程维护的控制连接映射表
4. 时间轮中的控制连接

因此，V5 的退出流程是一条完整的“全局清理链”，不是简单 `exit()`。

---

## 5. 客户端启动、登录与主循环活动图

客户端入口是 `src/client/client.c` 的 `main()`。

## 5.1 客户端启动与登录总流程

```mermaid
flowchart TD
    C0[进入 client main]
    C1[先按默认路径初始化日志]
    C2[读取 ip port log 配置]
    C3[按正式配置重新初始化日志]
    C4[signal SIGPIPE ignore]
    C5[初始化 ClientState]
    C6[初始化 test/client_files]
    C7[init_socket 建立控制连接]
    C8[发送 conn_init_packet CONN_ROLE_CTRL]
    C9[进入 client_login_menu]
    C10[显示未登录菜单]
    C11[读取用户输入]
    C12{输入是否为 quit}
    C13{输入是否为 login/register}
    C14[process_command]
    C15{是否 login 且登录成功}
    C16[进入已登录命令循环]
    C17[继续停留在未登录菜单]
    C18[客户端退出]

    C0 --> C1 --> C2 --> C3 --> C4 --> C5 --> C6 --> C7 --> C8 --> C9 --> C10 --> C11
    C11 --> C12
    C12 -- 是 --> C18
    C12 -- 否 --> C13
    C13 -- 否 --> C17 --> C10
    C13 -- 是 --> C14 --> C15
    C15 -- 是 --> C16
    C15 -- 否 --> C17
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class C0,C1,C2,C3 phase1
    class C4,C5,C6,C7 phase2
    class C8,C9,C10,C11 phase3
    class C12,C13,C14,C15 phase4
    class C16,C17 phase5
    class C18 fail
```

### 5.1.1 这里和 V4 的一个关键差异

`client.c` 里已经没有“控制连接失效后自动重连”的代码路径。

也就是说，V5 客户端主流程是：

1. 启动时建立一次控制连接
2. 登录成功后进入命令循环
3. 后续命令如果因为控制连接断开而失败，`main()` 不会自动重连

因此活动图里不能再画 V4 那条“重新建立主连接并重新登录”的链路。

---

## 5.2 已登录命令循环活动图

```mermaid
flowchart TD
    L0[进入已登录命令循环]
    L1[打印提示符]
    L2[fgets 读取一整行]
    L3{输入是否结束}
    L4[去掉换行符]
    L5{是否 quit/exit}
    L6{是否空行}
    L7[process_command]
    L8[继续下一轮循环]
    L9[退出客户端]

    L0 --> L1 --> L2 --> L3
    L3 -- 是 --> L9
    L3 -- 否 --> L4 --> L5
    L5 -- 是 --> L9
    L5 -- 否 --> L6
    L6 -- 是 --> L1
    L6 -- 否 --> L7 --> L8 --> L1
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class L0,L1 phase1
    class L2,L3 phase2
    class L4,L5 phase3
    class L6,L7 phase4
    class L8 phase5
    class L9 fail
```

### 5.2.1 这条流程里最重要的事实

V5 客户端主循环很直接：

1. 读输入
2. 交给 `process_command()`
3. 不做自动重连分支
4. 继续下一轮

因此，后续复杂度都转移到了 `process_command()` 和 `puts/gets` 各自的处理函数里。

---

## 6. 客户端命令分流活动图

客户端把命令分成“短命令链”和“长命令链”的位置，在 `src/client/client_command_handle.c` 的 `process_command()`。

```mermaid
flowchart TD
    P0[process_command 开始]
    P1[parse_input 拆分命令字和参数]
    P2[validate_command_args]
    P3{参数是否合法}
    P4[get_cmd_type]
    P5{命令是否合法}
    P6{是否 login}
    P7[handle_login_command]
    P8{是否 gets}
    P9[handle_gets_command]
    P10{是否 puts}
    P11[handle_puts_command]
    P12[handle_normal_command]
    P13[返回结果]

    P0 --> P1 --> P2 --> P3
    P3 -- 否 --> P13
    P3 -- 是 --> P4 --> P5
    P5 -- 否 --> P13
    P5 -- 是 --> P6
    P6 -- 是 --> P7 --> P13
    P6 -- 否 --> P8
    P8 -- 是 --> P9 --> P13
    P8 -- 否 --> P10
    P10 -- 是 --> P11 --> P13
    P10 -- 否 --> P12 --> P13
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class P0,P1,P2 phase1
    class P3,P4,P5 phase2
    class P6,P7,P8 phase3
    class P9,P10,P11 phase4
    class P12,P13 phase5
```

### 6.1 这张图为什么重要

因为这就是 V5 长短命令分离的**第一个分叉点**。

在这一步之前，所有命令都只是用户输入的一行字符串。  
从这一步开始，命令被拆成四类：

1. 非法命令
2. 登录命令
3. 长命令 `puts/gets`
4. 其它普通短命令

---

## 6.2 普通短命令活动图

```mermaid
flowchart TD
    N0[组装 command_packet_t]
    N1[控制连接 send_command_packet]
    N2{发送是否成功}
    N3[recv_server_reply]
    N4{接收是否成功}
    N5[打印服务端文本响应]
    N6{是否 cd 且返回成功}
    N7[update_client_path_after_cd]
    N8[返回]

    N0 --> N1 --> N2
    N2 -- 否 --> N8
    N2 -- 是 --> N3 --> N4
    N4 -- 否 --> N8
    N4 -- 是 --> N5 --> N6
    N6 -- 是 --> N7 --> N8
    N6 -- 否 --> N8
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class N0,N1 phase1
    class N2,N3 phase2
    class N4,N5 phase3
    class N6,N7 phase4
    class N8 phase5
```

这张图说明：

> 短命令虽然都走控制连接，但 `cd` 成功后还会额外修改客户端本地保存的远端路径。

---

## 7. 服务端 `epoll` 主循环活动图

这张图是 V5 里最关键的一张活动图，因为所有服务端连接最终都先经过这里。

```mermaid
flowchart TD
    M0[epoll_wait]
    M1{epoll_wait 是否出错}
    M2[遍历本轮全部就绪事件]
    M3{"fd 是否为 pipe_fd[0]"}
    M4[进入服务端退出流程]
    M5{fd 是否为 timer_fd}
    M6[handle_timer_event 推进时间轮]
    M7{fd 是否为 listen_fd}
    M8[accept 新连接]
    M9[recv_conn_init_packet]
    M10{role 是否为 CONN_ROLE_CTRL}
    M11[register_control_connection]
    M12{role 是否为 CONN_ROLE_TRANSFER}
    M13[传输连接 fd 入线程池队列]
    M14[handle_control_event]
    M15[继续下一事件]

    M0 --> M1
    M1 -- EINTR --> M0
    M1 -- 正常 --> M2 --> M3
    M3 -- 是 --> M4
    M3 -- 否 --> M5
    M5 -- 是 --> M6 --> M15
    M5 -- 否 --> M7
    M7 -- 是 --> M8 --> M9 --> M10
    M10 -- 是 --> M11 --> M15
    M10 -- 否 --> M12
    M12 -- 是 --> M13 --> M15
    M12 -- 否 --> M15
    M7 -- 否 --> M14 --> M15
    M15 --> M2
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class M0,M1,M2,M3 phase1
    class M5,M6 phase2
    class M7,M8,M9 phase3
    class M10,M11,M12 phase4
    class M13,M14,M15 phase5
    class M4 fail
```

## 7.1 这张图的核心意义

这张图说明 V5 服务端主线程同时承担了四件事：

1. 监听退出信号
2. 推进时间轮
3. 接收并分类新连接
4. 处理控制连接命令

和 V4 最大的差异在于：

> V5 不再需要靠“预读命令类型”区分长短命令，而是新连接一开始就先收 `conn_init_packet_t`，直接按连接角色分流。

---

## 8. 控制连接命令处理活动图

控制连接最终进入的是 `dispatch_control_command()`。

```mermaid
flowchart TD
    S0[dispatch_control_command]
    S1[读取 cmd_type]
    S2[复制可写参数 arg_buf]
    S3{是否未登录且不是 login/register}
    S4[send_msg 请先登录]
    S5{cmd_type 是什么}
    S6[handle_login]
    S7{是否首次登录成功}
    S8[ctx.current_path = /]
    S9[ctx.current_dir_id = 0]
    S10[handle_register]
    S11[handle_pwd]
    S12[handle_cd]
    S13[handle_ls]
    S14[handle_touch]
    S15[handle_rm]
    S16[handle_mkdir]
    S17[handle_rmdir]
    S18[handle_multi_gets_plan]
    S19[handle_gets_range_ticket_prepare]
    S20[handle_transfer_prepare]
    S21[send_msg 指令错误]
    S22[返回]

    S0 --> S1 --> S2 --> S3
    S3 -- 是 --> S4 --> S22
    S3 -- 否 --> S5
    S5 -- LOGIN --> S6 --> S7
    S7 -- 是 --> S8 --> S9 --> S22
    S7 -- 否 --> S22
    S5 -- REGISTER --> S10 --> S22
    S5 -- PWD --> S11 --> S22
    S5 -- CD --> S12 --> S22
    S5 -- LS --> S13 --> S22
    S5 -- TOUCH --> S14 --> S22
    S5 -- RM --> S15 --> S22
    S5 -- MKDIR --> S16 --> S22
    S5 -- RMDIR --> S17 --> S22
    S5 -- GETS --> S18 --> S22
    S5 -- GETS_RANGE --> S19 --> S22
    S5 -- PUTS --> S20 --> S22
    S5 -- 其它 --> S21 --> S22
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class S0,S1,S2,S3,S4 phase1
    class S5,S6,S7,S8,S9 phase2
    class S10,S11,S12,S13 phase3
    class S14,S15,S16,S17,S18 phase4
    class S19,S20,S22 phase5
    class S21 fail
```

### 8.1 这张图的关键点

#### A. V5 的 `gets` 在控制连接上不是“直接下载”

代码里：

```c
case CMD_TYPE_GETS:
    handle_multi_gets_plan(...)
```

这说明控制连接上的 `gets` 只负责：

1. 查目标逻辑文件
2. 查真实文件大小和 hash
3. 查可用数据源列表
4. 返回 `multi_gets_plan_packet_t`

#### B. `puts` 在控制连接上只负责签发票据

代码里：

```c
case CMD_TYPE_PUTS:
    handle_transfer_prepare(...)
```

也就是说，控制连接不会直接接收上传内容。

---

## 9. 传输连接票据校验与分发活动图

线程池工作线程处理传输连接时，入口是 `handle_transfer_request()`。

```mermaid
flowchart TD
    T0[handle_transfer_request]
    T1[recv_transfer_auth_packet]
    T2{认证包接收是否成功}
    T3[verify_and_consume_transfer_ticket]
    T4{票据是否有效}
    T5[get_current_server_address]
    T6{票据绑定的数据源地址是否匹配本服务器}
    T7[build_transfer_context]
    T8{是否成功恢复 ClientContext 和 file_name}
    T9{cmd_type 是否为 GETS}
    T10{range 是否有效}
    T11[handle_gets_range]
    T12[handle_gets]
    T13{cmd_type 是否为 PUTS}
    T14[handle_puts]
    T15{命令是否为 GETS}
    T16[fail_gets_request]
    T17[fail_puts_request]
    T18[失败后返回]

    T0 --> T1 --> T2
    T2 -- 否 --> T18
    T2 -- 是 --> T3 --> T4
    T4 -- 否 --> T18
    T4 -- 是 --> T5 --> T6
    T6 -- 否 --> T18
    T6 -- 是 --> T7 --> T8
    T8 -- 否 --> T15
    T15 -- 是 --> T16 --> T18
    T15 -- 否 --> T17 --> T18
    T8 -- 是 --> T9
    T9 -- 是 --> T10
    T10 -- 是 --> T11
    T10 -- 否 --> T12
    T9 -- 否 --> T13
    T13 -- 是 --> T14
    T13 -- 否 --> T18
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class T0,T1,T2,T3 phase1
    class T4,T5,T6,T7 phase2
    class T8,T9,T10,T11 phase3
    class T12,T13,T14,T15 phase4
    class T16,T17 phase5
    class T18 fail
```

### 9.1 这张图的核心意义

这张图说明 V5 的传输连接不是“连上就开始传文件”，而是必须先经过：

1. 一次性票据校验
2. 票据消费
3. 数据源地址匹配校验
4. 临时 `ClientContext` 恢复

之后才会进入上传或下载函数。

---

## 10. 上传 `puts` 活动图

V5 的上传拆成“控制连接申请票据 + 传输连接上传”两段。

## 10.1 客户端 `puts` 总流程

```mermaid
flowchart TD
    P0[handle_puts_command]
    P1[拼接本地文件路径 test/client_files/file]
    P2{本地文件路径是否合法}
    P3{本地文件是否存在}
    P4[request_transfer_ticket]
    P5{票据申请是否成功}
    P6[复制 server_ip/server_port]
    P7[pthread_create puts_thread_func]
    P8[pthread_detach]
    P9[打印 上传任务已启动]
    P10[返回]

    P0 --> P1 --> P2
    P2 -- 否 --> P10
    P2 -- 是 --> P3
    P3 -- 否 --> P10
    P3 -- 是 --> P4 --> P5
    P5 -- 否 --> P10
    P5 -- 是 --> P6 --> P7 --> P8 --> P9 --> P10
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class P0,P1,P2 phase1
    class P3,P4 phase2
    class P5,P6 phase3
    class P7,P8 phase4
    class P9,P10 phase5
```

## 10.2 上传线程执行流程

```mermaid
flowchart TD
    U0[puts_thread_func]
    U1[init_socket 建立传输连接]
    U2[发送 conn_init_packet CONN_ROLE_TRANSFER]
    U3{发送是否成功}
    U4[发送 transfer_auth_packet]
    U5{发送是否成功}
    U6[run_puts_transfer]
    U7[关闭传输连接]
    U8[free task]
    U9[线程结束]

    U0 --> U1 --> U2 --> U3
    U3 -- 否 --> U7
    U3 -- 是 --> U4 --> U5
    U5 -- 否 --> U7
    U5 -- 是 --> U6 --> U7
    U7 --> U8 --> U9
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class U0,U1 phase1
    class U2,U3 phase2
    class U4,U5 phase3
    class U6,U7 phase4
    class U8,U9 phase5
```

## 10.3 服务端上传流程

```mermaid
flowchart TD
    F0[handle_puts]
    F1[recv_file_packet]
    F2{接收文件信息是否成功}
    F3[build_full_virtual_path]
    F4{路径是否合法}
    F4A[send_msg 路径非法或过长]
    F5[extract_file_name]
    F6{文件名是否合法}
    F6A[send_msg 错误 文件名过长]
    F7[dao_get_node_by_path]
    F8{逻辑路径是否已存在}
    F9[返回 file_packet offset=file_size 和错误文本]
    F10{客户端是否提供 hash}
    F10A[send_msg 上传失败 客户端没有提供文件哈希值]
    F11[dao_file_find_by_sha256]
    F12{数据库是否已有同 hash}
    F13[check_store_file_ready]
    F14{校验结果是否为 -1}
    F15[send_msg 秒传失败 服务端无法校验真实文件]
    F16{真实文件是否完整可复用}
    F17[create_user_file_link]
    F18[返回 file_packet hash=相同 hash]
    F19[build_store_file_path]
    F20[open 真实文件]
    F21[计算 local_size]
    F22[返回服务端已有 offset]
    F23{remaining <= 0}
    F24[finish_upload_db_work]
    F25[reply 上传完成]
    F26{file_len == 0}
    F27[ftruncate 到目标总大小]
    F28{文件是否超过 100MB}
    F29[mmap + recv 接收剩余内容]
    F30[read/write 接收剩余内容]
    F31{是否完整收满}
    F32[ftruncate 到已有真实进度]
    F33[reply 传输中断 已保存当前进度]
    F34[finish_upload_db_work]
    F35[reply 上传完成]
    F36[结束]

    F0 --> F1 --> F2
    F2 -- 否 --> F36
    F2 -- 是 --> F3 --> F4
    F4 -- 否 --> F4A --> F36
    F4 -- 是 --> F5 --> F6
    F6 -- 否 --> F6A --> F36
    F6 -- 是 --> F7 --> F8
    F8 -- 是 --> F9 --> F36
    F8 -- 否 --> F10
    F10 -- 否 --> F10A --> F36
    F10 -- 是 --> F11 --> F12
    F12 -- 是 --> F13 --> F14
    F14 -- 是 --> F15 --> F36
    F14 -- 否 --> F16
    F16 -- 是 --> F17 --> F18 --> F36
    F16 -- 否 --> F19
    F12 -- 否 --> F19
    F19 --> F20 --> F21 --> F22 --> F23
    F23 -- 是 --> F24 --> F25 --> F36
    F23 -- 否 --> F26
    F26 -- 是 --> F24 --> F25 --> F36
    F26 -- 否 --> F27 --> F28
    F28 -- 是 --> F29 --> F31
    F28 -- 否 --> F30 --> F31
    F31 -- 否 --> F32 --> F33 --> F36
    F31 -- 是 --> F34 --> F35 --> F36
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class F0,F1,F2,F3,F4,F5,F6 phase1
    class F7,F8,F10,F11,F12 phase2
    class F13,F14,F16,F17,F18,F19,F20 phase3
    class F21,F22,F23,F24,F25,F26,F27,F28 phase4
    class F29,F30,F31,F32,F34,F35,F36 phase5
    class F4A,F6A,F9,F10A,F15,F33 fail
```

### 10.4 这张上传活动图最重要的三点

#### A. V5 的 `puts` 依然保留了第二期和第三期的断点续传骨架

也就是说，传输连接进入 `handle_puts()` 后，仍然是：

1. 客户端先发 `file_packet_t`
2. 服务端回一个 `offset`
3. 客户端从这个 `offset` 继续发剩余内容

#### B. V5 新增的一次性票据只影响“能不能进入上传函数”

它不推翻 `handle_puts()` 本身的文件传输骨架。

#### C. 上传完成后还要多维护 `file_sources`

因为第五期 `gets` 申请方案时，需要通过 `file_id` 查：

> 哪些服务器能提供这份真实文件

---

## 11. 下载 `gets` 活动图

V5 的 `gets` 是项目里流程最长的一条链。

## 11.1 客户端 `gets` 总流程

```mermaid
flowchart TD
    G0[handle_gets_command]
    G1[拼接最终本地文件路径]
    G2{文件名是否合法}
    G3[request_multi_gets_plan]
    G4{方案申请是否成功}
    G5[multipart_task_prepare]
    G6{任务目录和元数据是否准备成功}
    G7[multipart_task_sync_progress]
    G8{进度同步是否成功}
    G9[part_index = 0]
    G10{是否还有未检查的分片}
    G11{本分片是否已完成}
    G12[request_gets_range_ticket]
    G13{票据申请是否成功}
    G14[download_part_count 加 1]
    G15[part_index 加 1]
    G16[pthread_create gets_manager_thread_func]
    G17{后台线程是否创建成功}
    G18[pthread_detach]
    G19[打印 多点下载任务已启动]
    G20[返回]

    G0 --> G1 --> G2
    G2 -- 否 --> G20
    G2 -- 是 --> G3 --> G4
    G4 -- 否 --> G20
    G4 -- 是 --> G5 --> G6
    G6 -- 否 --> G20
    G6 -- 是 --> G7 --> G8
    G8 -- 否 --> G20
    G8 -- 是 --> G9 --> G10
    G10 -- 否 --> G16 --> G17
    G10 -- 是 --> G11
    G11 -- 是 --> G15 --> G10
    G11 -- 否 --> G12 --> G13
    G13 -- 否 --> G20
    G13 -- 是 --> G14 --> G15 --> G10
    G17 -- 否 --> G20
    G17 -- 是 --> G18 --> G19 --> G20
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class G0,G1,G2,G3,G4 phase1
    class G5,G6,G7,G8 phase2
    class G9,G10,G11,G12 phase3
    class G13,G14,G15,G16 phase4
    class G17,G18,G19,G20 phase5
```

## 11.2 下载管理线程流程

```mermaid
flowchart TD
    M0[gets_manager_thread_func]
    M1[遍历本轮要下载的分片]
    M2[pthread_create gets_part_thread_func]
    M3[join 全部分片线程]
    M4[multipart_task_sync_progress]
    M5{是否全部分片都完成}
    M6[打印 可再次执行 gets 继续下载]
    M7[merge_part_files]
    M8{合并是否成功}
    M9[get_file_sha256 校验最终文件]
    M10{整文件 hash 是否匹配}
    M11[multipart_task_cleanup]
    M12[打印 多点下载完成]
    M13[free task]
    M14[线程结束]

    M0 --> M1 --> M2 --> M3 --> M4 --> M5
    M5 -- 否 --> M6 --> M13 --> M14
    M5 -- 是 --> M7 --> M8
    M8 -- 否 --> M13 --> M14
    M8 -- 是 --> M9 --> M10
    M10 -- 否 --> M13 --> M14
    M10 -- 是 --> M11 --> M12 --> M13 --> M14
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class M0,M1,M2 phase1
    class M3,M4,M5 phase2
    class M6,M7,M8 phase3
    class M9,M10,M11 phase4
    class M12,M13,M14 phase5
```

## 11.3 单个分片下载线程流程

```mermaid
flowchart TD
    D0[gets_part_thread_func]
    D1[init_socket 建立传输连接]
    D2[发送 conn_init_packet CONN_ROLE_TRANSFER]
    D3{发送是否成功}
    D4[发送 transfer_auth_packet]
    D5{发送是否成功}
    D6[run_gets_part_transfer]
    D7[关闭传输连接]
    D8[线程结束]

    D0 --> D1 --> D2 --> D3
    D3 -- 否 --> D7 --> D8
    D3 -- 是 --> D4 --> D5
    D5 -- 否 --> D7 --> D8
    D5 -- 是 --> D6 --> D7 --> D8
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class D0,D1 phase1
    class D2,D3 phase2
    class D4,D5 phase3
    class D6,D7 phase4
    class D8 phase5
```

## 11.4 服务端下载流程

```mermaid
flowchart TD
    H0[handle_gets 或 handle_gets_range]
    H1[进入 handle_gets_common]
    H2[build_full_virtual_path]
    H3{路径是否合法}
    H4[send_gets_failed_packet]
    H5[dao_get_file_info_by_path]
    H6{逻辑文件是否存在}
    H7[dao_file_get_info_by_id]
    H8{files 信息是否存在}
    H9[build_store_file_path]
    H10[open 真实文件]
    H11{真实文件是否可打开}
    H12{是否为区间下载}
    H13{区间是否合法}
    H14[计算 logical_total_size]
    H15["先发送 file_packet(file_size)"]
    H16[接收客户端 offset]
    H17{offset 是否合法}
    H18[offset 重置为 0]
    H19{逻辑总长度是否超过 100MB}
    H20[mmap 发送剩余内容]
    H21[sendfile 发送剩余内容]
    H22[结束]

    H0 --> H1 --> H2 --> H3
    H3 -- 否 --> H4 --> H22
    H3 -- 是 --> H5 --> H6
    H6 -- 否 --> H4 --> H22
    H6 -- 是 --> H7 --> H8
    H8 -- 否 --> H4 --> H22
    H8 -- 是 --> H9 --> H10 --> H11
    H11 -- 否 --> H4 --> H22
    H11 -- 是 --> H12
    H12 -- 是 --> H13
    H13 -- 否 --> H4 --> H22
    H13 -- 是 --> H14
    H12 -- 否 --> H14
    H14 --> H15 --> H16 --> H17
    H17 -- 否 --> H18 --> H19
    H17 -- 是 --> H19
    H19 -- 是 --> H20 --> H22
    H19 -- 否 --> H21 --> H22
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class H0,H1,H2,H3,H4 phase1
    class H5,H6,H7,H8,H9 phase2
    class H10,H11,H12,H13 phase3
    class H14,H15,H16,H17,H18 phase4
    class H19,H20,H21,H22 phase5
```

### 11.5 这张下载活动图最重要的三点

#### A. V5 的 `gets` 分成两段

第一段是控制连接上的：

1. 下载方案申请
2. 区间票据申请

第二段才是传输连接上的：

1. 分片下载
2. 分片续传
3. 分片合并

#### B. 分片没下完时，不会强行删除现场

管理线程会保留：

1. `.multipart/<file_hash>/meta.ini`
2. 下载好的 `part_i`

这样下一次再次执行 `gets` 时，还能继续复用已有分片。

#### C. 传输连接上的 `gets` 依然复用了旧的文件发送骨架

也就是说，即使进入第五期，数据发送核心仍然是：

1. 服务端先发 `file_packet_t`
2. 客户端回传已完成偏移
3. 服务端从偏移继续发剩余字节

---

## 12. 工作线程执行传输任务流程

V5 的工作线程入口是 `src/server/core/worker.c` 的 `thread_func()`。

```mermaid
flowchart TD
    W0[工作线程启动]
    W1[加锁访问队列]
    W2{队列是否为空 且 exitFlag 为 0}
    W3[pthread_cond_wait]
    W4{exitFlag 是否为 1}
    W5[deQueue 取出 client_fd]
    W6[busy_fds 记录 fd]
    W7[解锁]
    W8[设置收发超时 30 秒]
    W9[handle_transfer_request]
    W10[shutdown client_fd]
    W11[close client_fd]
    W12[重新加锁]
    W13[busy_fds 置回 -1]
    W14[解锁]
    W15[进入下一轮]
    W16[线程退出]

    W0 --> W1 --> W2
    W2 -- 是 --> W3 --> W4
    W4 -- 是 --> W16
    W4 -- 否 --> W2
    W2 -- 否 --> W5 --> W6 --> W7 --> W8 --> W9 --> W10 --> W11 --> W12 --> W13 --> W14 --> W15 --> W1
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class W0,W1,W2,W3 phase1
    class W4,W5,W6 phase2
    class W7,W9,W10 phase3
    class W11,W12,W13 phase4
    class W14,W15 phase5
    class W8,W16 fail
```

这张图说明：

> 线程池在 V5 里只负责传输连接，不再负责控制连接。

---

## 13. 时间轮超时处理流程

时间轮相关逻辑分散在：

- `src/server/core/server.c`
- `src/server/net/time_wheel.c`

## 13.1 timerfd 驱动时间轮推进流程

```mermaid
flowchart TD
    T0[timer_fd 在 epoll 中就绪]
    T1[handle_timer_event]
    T2[read timerfd expired_count]
    T3{expired_count 是否大于 0}
    T4[time_wheel_tick]
    T5[current_tick 加 1]
    T6[定位本轮槽位]
    T7[取出本槽位头节点]
    T8{本槽位是否还有节点}
    T9[保存 next 指针]
    T10{conn.expire_tick 是否到期}
    T11[从时间轮链表中摘除]
    T12[清空 conn.wheel_node 和 wheel_slot]
    T13[handle_control_timeout]
    T14[free 本时间轮节点]
    T15[跳到 next 节点]
    T16[expired_count 减 1]
    T17[返回 epoll 主循环]

    T0 --> T1 --> T2 --> T3
    T3 -- 否 --> T17
    T3 -- 是 --> T4 --> T5 --> T6 --> T7 --> T8
    T8 -- 否 --> T16 --> T3
    T8 -- 是 --> T9 --> T10
    T10 -- 是 --> T11 --> T12 --> T13 --> T14 --> T15 --> T8
    T10 -- 否 --> T15 --> T8
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class T0,T1,T2,T3 phase1
    class T4,T5,T6,T7 phase2
    class T8,T9,T10 phase3
    class T11,T12,T13,T14 phase4
    class T15,T16,T17 phase5
```

## 13.2 控制连接在时间轮中的生命周期

```mermaid
flowchart TD
    C0[accept 新连接]
    C1[recv_conn_init_packet role=CTRL]
    C2[register_control_connection]
    C3[time_wheel_add]
    C4[等待控制命令]
    C5[收到一条控制命令]
    C6[dispatch_control_command]
    C7[time_wheel_refresh]
    C8[继续等待下一条命令]
    C9{到期前是否还有新命令}
    C10[handle_control_timeout]
    C11[close_control_connection]
    C12[生命周期结束]

    C0 --> C1 --> C2 --> C3 --> C4 --> C5 --> C6 --> C7 --> C8 --> C9
    C9 -- 是 --> C5
    C9 -- 否 --> C10 --> C11 --> C12
    classDef phase1 fill:#E8F3FF,stroke:#2F6FB6,color:#102A43
    classDef phase2 fill:#FFF1D6,stroke:#C77700,color:#4A2B00
    classDef phase3 fill:#EFFFFA,stroke:#00897B,color:#003D35
    classDef phase4 fill:#E9F8EF,stroke:#2E7D32,color:#173B1A
    classDef phase5 fill:#F3ECFF,stroke:#7C4DFF,color:#2D1B69
    classDef fail fill:#FFECEC,stroke:#C62828,color:#4A0D0D

    class C0,C1,C2 phase1
    class C3,C4,C5 phase2
    class C6,C7 phase3
    class C8,C9,C10 phase4
    class C11,C12 phase5
```

### 13.3 时间轮流程的关键点

#### A. 时间轮只服务控制连接

传输连接不会被挂到时间轮上。

#### B. 超时推进不是手写 `sleep`

而是：

1. `timerfd` 每秒触发
2. `epoll` 收到 `timerfd` 可读
3. `handle_timer_event()` 推进时间轮

#### C. 控制连接每处理完一条命令，都会刷新超时位置

这一点在 `handle_control_event()` 里是显式存在的：

```c
dispatch_control_command(...);
time_wheel_refresh(wheel, conn);
```

---

## 14. 总结

V5 的活动图骨架可以概括成一句话：

> 客户端所有命令先在 `process_command()` 分流，服务端所有新连接先在 `recv_conn_init_packet()` 分流，控制连接由主线程 `epoll + 时间轮` 管理，传输连接由线程池执行，而第五期 `gets` 又在控制阶段和传输阶段之间多插入了一层“方案申请 + 分片票据申请”流程。
